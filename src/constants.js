'use strict';

const USERS_API = '/problems-svc/api/v1.0';

const serviceConfig = {
  serviceName: 'problems-svc',
  HOST: 'localhost',
  PORT: 5200,
  PROTOCOL: 'http',
};

export { USERS_API, serviceConfig };
